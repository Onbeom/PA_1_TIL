인스턴스  시작 > 보안그룹 HTTP/SSH 추가 > 퍼블릭 IP (변경됨) or 탄력적 IP (불변)으로 접속

> sudo apt update  
> sudo apt install nginx  


> sudo cp "FILE NAME" /var/www/html/index.nginx-debian.html   
> sudo service nginx start  
